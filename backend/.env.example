DB_USER=postgres
DB_PASSWORD=finalpro
DB_HOST=localhost
DB_PORT=5432
DB_NAME=connect_startup
# Set DB_SSL=true for Render/external Postgres (auto-enabled when DB_HOST contains render.com)
# DB_SSL=true
JWT_SECRET=replace-with-a-long-random-secret
PORT=5001
FRONTEND_URL=https://startup-connect-ethiopia-frontend.onrender.com
CHAPA_PUBLIC_KEY=
CHAPA_SECRET_KEY=
CHAPA_RETURN_URL=https://startup-connect-ethiopia-frontend.onrender.com/investor/payment/completed
CHAPA_CALLBACK_URL=http://localhost:5001/api/payments/callback
CHAPA_BASE_URL=https://api.chapa.co/v1
OPENAI_API_KEY=
GROQ_API_KEY=
GOOGLE_CLIENT_ID=
# Brevo email — use SMTP keys (xsmtpsib) OR HTTP API key (xkeysib), not both mixed up
# 1. Settings -> Senders -> verify your Gmail (e.g. etsubgirma0312@gmail.com)
# 2. Settings -> SMTP & API -> copy SMTP login + SMTP key (xsmtpsib-...)
BREVO_SMTP_USER=ad1645001@smtp-brevo.com
BREVO_SMTP_KEY=
BREVO_FROM_EMAIL=etsubgirma0312@gmail.com
BREVO_FROM_NAME=StartupConnect

# Optional: HTTP API key (xkeysib-...) from Brevo -> API Keys — different from SMTP key
BREVO_API_KEY=

# Resend (https://resend.com) — onboarding@resend.dev only sends to your Resend signup email
# For all users, verify your own domain at resend.com/domains
RESEND_API_KEY=
RESEND_FROM_EMAIL=StartupConnect <onboarding@resend.dev>

# Local/dev fallback (Gmail works locally; often fails on Render)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
